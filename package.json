{
  "name": "gumtree-notifier",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "aws-sdk": "^2.426.0",
    "axios": "^0.21.1",
    "puppeteer": "^1.13.0"
  }
}